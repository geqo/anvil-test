// Enums.cs
using System;

public enum PawnType { Pawn, Animal, Robot }
public enum PawnState { Idle, Moving, Sleeping, Eating, Working }
public enum NeedType { Energy, Sanity, Mood, Boredom, Hunger }
